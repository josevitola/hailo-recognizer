[project]
name = "recognizer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jose Vitola", email = "jdnv.123@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.11.1",
    "picamera2>=0.3.37",
    "scikit-learn>=1.9.0",
]

[project.scripts]
preview = "recognizer:main"
enroll = "scripts.enroll:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
